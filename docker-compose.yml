version: "3.9"

services:
  adgenai:
    build: .
    ports:
      - "8000:8000"
      - "3000:3000"
    env_file: .env
    volumes:
      - ./storage:/app/storage
    restart: unless-stopped

  # Optional: Redis for production job queue
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis-data:/data

# volumes:
#   redis-data:
